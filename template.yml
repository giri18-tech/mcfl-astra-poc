AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: Litigation API — FastAPI on Lambda with CloudWatch logging and VPC support.

# ---------------------------------------------------------------------------
# Parameters
# ---------------------------------------------------------------------------
Parameters:

  # --- VPC (required when SQL Server is reached over Direct Connect / VPN) ---
  # Leave blank to deploy without VPC (DB will fall back to mock data).
  # Format: "subnet-aaa111,subnet-bbb222"
  VpcSubnetIds:
    Type: String
    Default: ""
    Description: Comma-separated private subnet IDs for Lambda VPC placement

  # Security group must allow outbound TCP 1433 to the SQL Server.
  VpcSecurityGroupIds:
    Type: String
    Default: ""
    Description: Comma-separated security group IDs (outbound TCP 1433 required)

  # --- DB config from SSM Parameter Store (resolved at deploy time) ----------
  # Pre-create with: aws ssm put-parameter --name /litigation-api/db-host --value "OEG-WXCASSQL203" --type String
  DbHost:
    Type: AWS::SSM::Parameter::Value<String>
    Default: /litigation-api/db-host

  DbPort:
    Type: AWS::SSM::Parameter::Value<String>
    Default: /litigation-api/db-port

  DbName:
    Type: AWS::SSM::Parameter::Value<String>
    Default: /litigation-api/db-name

  DbUser:
    Type: AWS::SSM::Parameter::Value<String>
    Default: /litigation-api/db-user

  # --- Log retention ----------------------------------------------------------
  LogRetentionDays:
    Type: Number
    Default: 30
    AllowedValues: [1, 3, 5, 7, 14, 30, 60, 90, 180, 365]
    Description: CloudWatch log retention in days

# ---------------------------------------------------------------------------
# Conditions
# ---------------------------------------------------------------------------
Conditions:
  HasVpc: !Not [!Equals [!Ref VpcSubnetIds, ""]]

# ---------------------------------------------------------------------------
# Globals
# ---------------------------------------------------------------------------
Globals:
  Function:
    Timeout: 30
    MemorySize: 512
    Runtime: python3.12
    Architectures:
      - x86_64

# ---------------------------------------------------------------------------
# Resources
# ---------------------------------------------------------------------------
Resources:

  # --- IAM Role --------------------------------------------------------------
  LitigationApiFunctionRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub ${AWS::StackName}-lambda-role
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        # Basic execution includes CloudWatch Logs: CreateLogGroup/Stream, PutLogEvents
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
        # VPC access (ENI management) — only attached when VPC is configured
        - !If
          - HasVpc
          - arn:aws:iam::aws:policy/service-role/AWSLambdaVPCAccessExecutionRole
          - !Ref AWS::NoValue
      Policies:
        # Allows Lambda to fetch secrets at runtime if code is updated to use boto3.
        # Currently DB_PASSWORD is injected via Secrets Manager dynamic reference at deploy time.
        - PolicyName: SecretsManagerRead
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action: secretsmanager:GetSecretValue
                Resource: !Sub arn:aws:secretsmanager:${AWS::Region}:${AWS::AccountId}:secret:litigation-api/*

  # --- CloudWatch Log Groups -------------------------------------------------
  # Explicit resource ensures retention policy is applied even before first invocation.
  LitigationFunctionLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub /aws/lambda/${AWS::StackName}-api
      RetentionInDays: !Ref LogRetentionDays

  HttpApiLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub /aws/apigateway/${AWS::StackName}
      RetentionInDays: !Ref LogRetentionDays

  # --- API Gateway (HTTP API) ------------------------------------------------
  HttpApi:
    Type: AWS::Serverless::HttpApi
    Properties:
      StageName: $default
      # Structured access logs sent to CloudWatch
      AccessLogSettings:
        DestinationArn: !GetAtt HttpApiLogGroup.Arn
        Format: >-
          {"requestId":"$context.requestId",
           "ip":"$context.identity.sourceIp",
           "method":"$context.httpMethod",
           "path":"$context.path",
           "status":"$context.status",
           "responseLatencyMs":"$context.responseLatency",
           "integrationLatencyMs":"$context.integrationLatency",
           "userAgent":"$context.identity.userAgent"}

  # --- Lambda Function -------------------------------------------------------
  LitigationApiFunction:
    Type: AWS::Serverless::Function
    DependsOn: LitigationFunctionLogGroup
    Metadata:
      BuildMethod: python3.12
      BuildProperties:
        RequirementsFile: requirements.txt
    Properties:
      # Explicit name → predictable log group: /aws/lambda/<stack>-api
      FunctionName: !Sub ${AWS::StackName}-api
      CodeUri: src/
      Handler: lambda_handler.handler
      Role: !GetAtt LitigationApiFunctionRole.Arn
      # VPC placement — required for on-prem SQL Server via Direct Connect / VPN.
      # Remove or leave VpcSubnetIds blank to deploy outside a VPC (mock data only).
      VpcConfig: !If
        - HasVpc
        - SubnetIds: !Split [",", !Ref VpcSubnetIds]
          SecurityGroupIds: !Split [",", !Ref VpcSecurityGroupIds]
        - !Ref AWS::NoValue
      Environment:
        Variables:
          # DB connection — values resolved from SSM at deploy time
          DB_HOST: !Ref DbHost
          DB_PORT: !Ref DbPort
          DB_NAME: !Ref DbName
          DB_USER: !Ref DbUser
          # DB_PASSWORD resolved from Secrets Manager at deploy time.
          # Pre-create: aws secretsmanager create-secret \
          #   --name litigation-api/db-password \
          #   --secret-string '{"password":"YOUR_PASSWORD_HERE"}'
          DB_PASSWORD: "{{resolve:secretsmanager:litigation-api/db-password:SecretString:password}}"
          LOG_LEVEL: INFO
      Events:
        # Root path (e.g. GET /)
        ApiRoot:
          Type: HttpApi
          Properties:
            ApiId: !Ref HttpApi
            Path: /
            Method: ANY
        # All other paths (e.g. GET /litigation, GET /litigation/cases/123)
        ApiProxy:
          Type: HttpApi
          Properties:
            ApiId: !Ref HttpApi
            Path: /{proxy+}
            Method: ANY

# ---------------------------------------------------------------------------
# Outputs
# ---------------------------------------------------------------------------
Outputs:
  HttpApiUrl:
    Description: Invoke URL — append paths such as /litigation?orbisId=12345
    Value: !Sub https://${HttpApi}.execute-api.${AWS::Region}.amazonaws.com

  FunctionArn:
    Description: Lambda function ARN
    Value: !GetAtt LitigationApiFunction.Arn

  FunctionLogGroup:
    Description: Lambda application log group (CloudWatch Logs)
    Value: !Sub /aws/lambda/${AWS::StackName}-api

  ApiGatewayLogGroup:
    Description: API Gateway access log group (CloudWatch Logs)
    Value: !Sub /aws/apigateway/${AWS::StackName}

  LambdaRoleArn:
    Description: IAM role ARN attached to the Lambda function
    Value: !GetAtt LitigationApiFunctionRole.Arn
