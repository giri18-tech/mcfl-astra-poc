# Secrets — never commit
src/.env
*.env
!src/dev.env

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/

# SAM build output
.aws-sam/
packaged-template.yaml
samconfig.toml.bak

# samconfig.toml contains DbPassword — do not commit with real password.
# Commit only when DbPassword=REPLACE_WITH_REAL_PASSWORD (placeholder).
# Better: pass password at deploy time via --parameter-overrides.

# macOS
.DS_Store

# IDE
.vscode/
.idea/
*.iml
